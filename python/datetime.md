```py
import datetime as dt
# now is an object
now = dt.datetime.now()
year = now.year
month = now.month
day_of_week = now.weekday()

# Set up a datetime object
date_of_birth = dt.datetime(year=1994, month=1, day=22)
```
